/* Empty values keep optional integrations off. See README.md. */
window.PORTFOLIO_CONFIG = Object.freeze({
  // Set true only after adding assets/demo/dashboard-hero.webp.
  HERO_DASHBOARD: false,
  // Full GoatCounter count URL, e.g. https://YOUR-SITE.goatcounter.com/count
  ANALYTICS_ENDPOINT: ''
});
